import { Head, Link, router } from '@inertiajs/react';
import { ChevronLeft, ChevronRight, Filter, Search, SlidersHorizontal, Users } from 'lucide-react';
import { useState } from 'react';

import { StatusBadge } from '@/components/StatusBadge';
import AppLayout from '@/layouts/AppLayout';
import { formatMoney } from '@/lib/format';
import type { Customer, PageProps, Paginator } from '@/types';

type Props = PageProps & { customers: Paginator<Customer>; filters: { search?: string; status?: string } };

export default function CustomersIndex({ customers, filters }: Props) {
    const [search, setSearch] = useState(filters.search ?? '');
    const submitSearch = (event: React.FormEvent) => { event.preventDefault(); router.get('/customers', { search: search || undefined, status: filters.status || undefined }, { preserveState: true, replace: true }); };

    return <AppLayout><Head title="Customers" /><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">Subscriber CRM</p><h1 className="page-title">Customers</h1><p className="page-subtitle">A clear view of everyone you keep connected.</p></div><Link href="/customers/create" className="button-primary"><Users size={17} />Add customer</Link></div><div className="mt-8 card overflow-hidden"><div className="flex flex-col gap-4 border-b border-line px-5 py-4 md:flex-row md:items-center md:justify-between"><form onSubmit={submitSearch} className="relative max-w-md flex-1"><Search size={17} className="pointer-events-none absolute start-3 top-3 text-muted" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search name, phone or customer code" className="field ps-10" /></form><div className="flex items-center gap-2"><button className="button-secondary"><Filter size={16} />Filters</button><button className="button-secondary"><SlidersHorizontal size={16} />Columns</button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-start"><thead><tr className="border-b border-line bg-sand/50 text-xs font-semibold uppercase tracking-wider text-muted"><th className="px-5 py-3.5 text-start">Customer</th><th className="px-5 py-3.5 text-start">Zone</th><th className="px-5 py-3.5 text-start">Services</th><th className="px-5 py-3.5 text-start">Balance</th><th className="px-5 py-3.5 text-start">Status</th><th className="px-5 py-3.5" /></tr></thead><tbody className="divide-y divide-line">{customers.data.map((customer) => <tr key={customer.public_id} className="group transition hover:bg-sand/30"><td className="px-5 py-4"><Link href={`/customers/${customer.public_id}`} className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full bg-brand-soft text-xs font-bold text-brand">{customer.first_name.slice(0, 1)}{customer.last_name?.slice(0, 1) ?? ''}</span><span><span className="block text-sm font-semibold group-hover:text-brand">{customer.first_name} {customer.last_name}</span><span className="mt-0.5 block text-xs text-muted">{customer.code} · {customer.phone}</span></span></Link></td><td className="px-5 py-4 text-sm text-muted">{customer.zone?.name ?? 'Unassigned'}</td><td className="px-5 py-4 text-sm text-muted">{customer.services.length} {customer.services.length === 1 ? 'service' : 'services'}</td><td className="px-5 py-4 text-sm font-semibold">{formatMoney(customer.balance_amount, customer.balance_currency)}</td><td className="px-5 py-4"><StatusBadge status={customer.status} /></td><td className="px-5 py-4 text-end"><Link href={`/customers/${customer.public_id}`} className="text-sm font-semibold text-brand opacity-0 transition group-hover:opacity-100">Open</Link></td></tr>)}{customers.data.length === 0 && <tr><td colSpan={6} className="px-5 py-16 text-center"><Users className="mx-auto text-muted" size={28} /><p className="mt-3 font-semibold">No customers found</p><p className="mt-1 text-sm text-muted">Try a different search or add your first customer.</p></td></tr>}</tbody></table></div><div className="flex items-center justify-between border-t border-line px-5 py-4"><p className="text-xs text-muted">Showing {customers.data.length ? (customers.current_page - 1) * customers.per_page + 1 : 0}–{Math.min(customers.current_page * customers.per_page, customers.total)} of {customers.total}</p><div className="flex items-center gap-1">{customers.links.map((link, index) => { const isPrevious = index === 0; const isNext = index === customers.links.length - 1; if (!link.url) return <span key={index} className="grid size-8 place-items-center text-muted/40">{isPrevious ? <ChevronLeft size={16} /> : isNext ? <ChevronRight size={16} /> : link.label}</span>; return <Link key={index} href={link.url} className={`grid size-8 place-items-center rounded-lg text-xs ${link.active ? 'bg-brand text-white' : 'text-muted hover:bg-sand'}`}>{isPrevious ? <ChevronLeft size={16} /> : isNext ? <ChevronRight size={16} /> : link.label}</Link>; })}</div></div></div></AppLayout>;
}
