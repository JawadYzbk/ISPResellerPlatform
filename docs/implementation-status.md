# Implementation status

Last updated: 2026-08-13

## Delivered

| Ticket | Slice | Status |
|---|---|---|
| ISP-001 | Laravel 13 runtime, React/Inertia frontend, development Docker Compose, production-shaped PHP-FPM/Nginx Docker Compose, CI with dependency audits, PHP/JavaScript formatting, PCOV coverage output, API-contract artifacts, and scheduled Chromium browser acceptance | foundation delivered; automated production-shaped container smoke passes migrations, cache compilation, preflight, health, and WhatsApp bridge readiness; production infrastructure acceptance remains |
| ISP-002 | Tenant context and integer money primitive | foundation delivered |
| ISP-003 | Initial architecture conventions and tests | foundation delivered |
| ISP-010 | Tenant, branch, zone and document-sequence schema, tenant provisioning, and audited platform-operator tenant CRUD | foundation delivered; platform workspace is available at `/admin/tenants`, while cross-tenant break-glass access remains intentionally separate |
| ISP-013 | Model scope plus route-binding isolation proof | foundation delivered |
| ISP-014 | RTL-aware Inertia app shell and staff navigation | foundation delivered; English, Arabic and French locale selection is supported, with RTL direction applied to the shell and documents |
| ISP-020/021 | Customer seed, index/show screen, staff create/edit flows, atomic subscriber registration with optional pending service and installation work order, tenant-safe zone/status/expiry filters and selectable columns, personal saved views, cross-page bulk selection and CSV export, real activity timeline, persisted provisioning details, recent support-ticket context, tenant-isolated search entry point, browser GPS capture, Leaflet/OpenStreetMap map picker/detail view, tenant-private customer document upload/detail/download with document classification and retention enforcement, and permission-aware empty-workspace setup guidance | vertical slice delivered |
| ISP-010/011/012/015 | Tenant provisioning and capability-gated settings, operator directory/invitations, capability catalog, seeded owner/admin reconciliation, static staff permission-matrix regression coverage, Sanctum tokens, password reset, 2FA, re-authentication, session devices and audit events | delivered for current tenant/operator scope |
| ISP-022/023/024 | Versioned plan prices, tenant-safe plan catalog/create workflow, addon and promotion CRUD, router rate-limit preview, customer creation/phone normalization, service activation/suspension/pause/resume/termination transitions, immediate/next-cycle service plan changes with proration and billing-period arithmetic | delivered for current catalog scope |
| ISP-030/031/032/033 | Historical FX with capability-gated effective-dated rate administration, inverse-rate lookup, opt-in Frankfurter ingestion for USD/LBP and other configured pairs, explicit half-up/floor/ceil conversion policies, configurable rate-freshness warnings, multi-currency collection with payment FX snapshots and audited overrides, append-only double-entry ledger, plan-priced and operator-created invoice lifecycles with immutable line/currency snapshots, append-only credit notes with a tenant-scoped reconciliation index, idempotent payments and reversals, staff invoice/payment queues, tenant-safe invoice/receipt details, A4 invoice/receipt PDF downloads with RTL document direction, browser print layouts, allocation-backed invoice balances and recent-auth reversal controls, and legacy currency-role reconciliation on migration and settings changes | delivered for current billing scope; treasury policy and provider availability remain operational gates |
| ISP-033B | Configurable Stripe PaymentIntent adapter, customer portal checkout, CSP allowlisting, raw-body signature verification, provider-object idempotency, invoice/customer/currency validation, and settlement through the existing payment ledger action | repository integration delivered; live Stripe credentials, webhook delivery and production settlement remain external acceptance gates |
| ISP-033A | Standalone PHP Whish Pay client, sandbox/production adapter, authenticated collector web QR screen with status polling, persisted collector payment attempts, SVG QR collection links, provider-verified callbacks, amount/currency validation, LBP whole-unit conversion, race-safe idempotent creation, serialized settlement callbacks, and idempotent `RecordPayment` settlement | repository integration delivered; current official merchant contract, sandbox credentials, callback delivery and production settlement remain external acceptance gates |
| ISP-034/035/036 | Payment renewal, invoice-first staff renewal desk with open-invoice reuse, signed multi-period renewal previews, current-cashier cash-shift reconciliation with variance controls, manager-level daily collector totals, and scheduled tenant-aware idempotent billing runs | delivered for current billing scope |
| ISP-041/043/044 | Driver boundary, post-commit network outbox, stale guards with 10s/60s/300s retry backoff, overdue suspension and reactivation, authorized service re-sync, network command queue, router operations health surface, dedicated service diagnostics, live session desk, incident index/detail and RouterOS comment/device-reference hardening | delivered with manual/fake drivers plus a configured external OSS/UISP/ACS webhook adapter; real endpoint and device acceptance remain external |
| ISP-050/051/052 | Template rendering, idempotent message queueing, provisioned localized defaults, controlled owner test delivery, configured WhatsApp Cloud/Web.js/SMS/FCM/email adapter seams, signed delivery callbacks, persistent multi-account Web.js QR/session bridge with durable idempotency, tenant-scoped account job routing, controlled disconnect/re-pair and explicit account deletion, tenant-local expiry reminders using the shared channel fallback path, customer welcome, payment receipt, service suspension/reactivation notices with opt-out/channel selection and configured-channel fallback, plus scoped zone/POP outage broadcasts | delivered at adapter/automation level; provider credentials, QR pairing and WhatsApp account acceptance remain external |
| ISP-060/061/062 | Ticket lifecycle, tenant-timezone business-hours SLA clock, scheduled resolved-ticket auto-close, tenant-safe staff ticket queue/detail/reply/status/assignment workflow, public versus internal message visibility, customer portal ticket messages, work-order queue/detail/history/checklist/completion/rescheduling/calendar, assigned work-order media evidence, signature capture, technician readings, bulk-material consumption, serialized inventory assignment, serialized-equipment return on service termination, warehouse transfer/recovery, bulk low-stock alerts, and combined serialized/bulk movement audit reporting | delivered for current ticket/work-order/inventory scope |
| ISP-070/071/072 | Sanctum API with role-scoped abilities, cursor pagination, query whitelists, idempotency middleware, app version config, tenant-scoped and host/session-resolved customer OTP/session authentication with server-side revocation, profile/services/usage/notices/tickets/billing APIs, balance/quota/restart/profile controls in the customer portal UI, separate customer balance/invoice/payment resources with PDF downloads, customer-owned rate-limited session restart, FX-aware payment and collector batches, collector cash-shift open/close/reconciliation, due/overdue customer desk, receipt resend and daily-summary endpoints, offline bootstrap/delta/push sync, responsive mobile field fallback navigation, session-authenticated `/field` collector desk with tenant/user-scoped AES-GCM encrypted IndexedDB/localStorage customer snapshot and currency catalog cache, queued payment persistence, confirmed device-data clearing and automatic push on reconnect, service discovery/state actions, plan-change previews/updates, signed multi-period renewal previews/invoice issuance, OpenAPI route-parity contract coverage, and the `/docs/api` contract endpoint | delivered for current customer/payment/service/collector/portal scope; OS-backed native encrypted storage and an offline authenticated app shell remain external client gates |
| ISP-073 | Technician-assigned work-order list/detail/status/failure endpoints, tenant-safe staff work-order completion surface, idempotent completion, assigned work-order evidence upload/detail response, signature capture, readings and bulk-material consumption, private operator download, service diagnostics and serialized inventory operations/assignment view | delivered for current technician scope |
| ISP-075 | Shared operator API completion: staff authentication/device controls, cursor-paginated customer/service/plan/invoice/payment/ticket/router/work-order/zone resources, customer/service/ticket/router creation, ticket replies/status transitions, tenant-scoped validation, capability gates, redacted resources and OpenAPI route-parity coverage | delivered for current v1 API scope |
| ISP-074 | Tenant-private service status broadcast events with after-commit dispatch, public-ID channel authorization, Laravel Reverb transport configuration and authenticated React client refresh bridge | delivered in repository; TLS, process supervision and runtime rollout remain operations gates |
| ISP-080/081/082/083 | Stock FreeRADIUS SQL schema (`radcheck`, `radreply`, `radgroupcheck`, `radgroupreply`, `radusergroup`, `radacct`, `radpostauth`, `nas`) with tenant/service extensions, router-sourced NAS runtime records, encrypted source router/CoA settings, UDP CoA/Disconnect client with response validation, scheduled `radacct` synchronization into current sessions, adapter-neutral NAS reboot session closure, scheduled stale-session cleanup, daily usage rollups, cycle quota/FUP command foundation, RouterOS FUP profile driver path and idempotent warning notifications | foundation delivered; CoA driver path delivered, lab acceptance pending |
| ISP-040/063 | POP/router inventory and capability-gated onboarding, tenant-safe POP and upstream-link inventory/detail/write workflows, tenant-safe IP pool/address inventory, tenant-safe router detail/update, encrypted connection tests, repeated-failure incidents, bounded router health observations and tenant-safe staff router operations page | delivered for current registry/write scope; real RouterOS lab acceptance remains |
| ISP-090/091 | Partner hierarchy/wallets with descendant-scoped API, effective price books, immutable commission accruals, settlement statements, tenant finance and operations reports with CSV/XLSX export, aging, collection rate, plan/zone/POP revenue, upstream cost/margin, collector performance, retention and usage breakdowns | delivered for current commercial/reporting scope |
| ISP-092 | Deferred owner dashboard finance metrics and six-month service-status trend, manager attention queue with deep links including expiring supplier credentials, live NOC signal panels for routers, sessions, commands, drift and incidents, plus the capability-aware global command palette | delivered for current dashboard scope |
| ISP-093 | Customer, plan, service, serialized-equipment and journal-backed balance CSV/XLSX import preview, row-level validation, partial-success commit and guarded/reversing rollback API, plus RouterOS PPP subscriber discovery import with redacted reports and a capability-gated staff import workspace | delivered for current tabular/router-discovery scope |
| ISP-P1-01 | Supplier inventory, contracts, bills, payment records, import, assignment, lifecycle driver, commercial batch capture, period reconciliation, audited reveal, journal-linked supplier bill/payment posting, and as-of supplier payable aging/settlement workspace | delivered for the current repository scope; external supplier acceptance remains |
| ISP-100 | Security headers, dependency audits, session hardening, transactional APP_KEY credential re-encryption command and audited customer PII anonymization | key rotation and PII anonymization delivered; disposable local APP_KEY rotation rehearsal passed; coordinated staging rotation, permission-matrix proof and remaining launch controls remain |
| ISP-011 | Capability seeding now reconciles legacy tenant users whose `role` column contains any supported role, including the full permission set and two-factor policy | delivered; run `php artisan db:seed --class=CapabilitySeeder` once on an existing environment |
| ISP-102/103 | Encrypted backup package configuration, dependency health, scheduler/queue-worker heartbeats, monitored scheduled-task check-ins, configurable queue-depth alerting, daily ledger invariant checks and privacy-safe Sentry wiring | repository wiring delivered; local encrypted SQLite and disposable Docker PostgreSQL/MinIO backup/restore rehearsals passed, distinct degraded signals are monitored, and production smoke validation is automated; off-site retention, production secret recovery, second-person restore and external alert routing remain |
| ISP-101 | Lazy Inertia page chunks and production frontend bundle audit | coherent 200-customer demo seed plus reproducible 50k-service seed/benchmark commands delivered; isolated PostgreSQL 18.4 and PostgreSQL 17 50k data/query-plan benchmarks passed; production-shaped pool and external cold-cache acceptance remain |
| ISP-104 | Administrator, operator and field-app guides, mobile API handover, deployment runbook and incident runbook, seeded staff browser acceptance journeys, owner-facing pilot readiness checklist, production release preflight command, and read-only external provider connectivity probe | repository documentation, screenshot-led local operator walkthrough, deterministic `npm run e2e` Chromium checks for login, partner workspace, customer/settings/readiness access, RTL layout, deterministic release configuration checks, provider probe coverage, and automated production-shaped smoke deployment delivered; approved-tenant execution, production sign-off and pilot handover remain |
| ISP-FB-01 | Lebanon-style collector territories with explicit all-zone or restricted coverage, hierarchical child-zone inclusion, effective-dated assignment history, manager settings UI, and server-side enforcement across field sync and collector customer APIs | delivered; scheduled temporary coverage and route execution remain in the Jebaya roadmap |
| ISP-FB-02 | Collector field-day check-in/out with explicit browser geolocation consent, bounded coordinate and accuracy validation, one-active-day lifecycle, tenant-timezone manager supervision, map handoff and no continuous background tracking | delivered for the responsive web field desk; native background/location policies remain deferred with the mobile application |
| ISP-FB-03 | Tenant-timezone daily collector route planning with territory-safe customer assignment, manager-defined stop order, guarded route replacement, field-desk queue, explicit nearby sorting, GPS-backed visit outcomes, same-day/active-shift enforcement and route progress/completion summaries | delivered for responsive web; native offline/background route execution remains deferred with the mobile application |
| ISP-FB-04 | Priority/due-date collector tasks with optional customer context, manager queue/filter/detail UI, ordered collector status transitions, participant-only task conversations, per-user unread state, tenant-private PDF/image/text attachments, and persisted checkout summaries covering time, route outcomes, collections, tasks, cash-shift state and handover notes | delivered for responsive web; native push notifications and background attachment synchronization remain deferred with the mobile application |

## In progress

- Real RouterOS/CoA integration against a CHR lab remains an acceptance gate; scheduled subscriber reconciliation reports drift, while device-side disablement has an explicit opt-in healing command.
- Real provider credentials/approvals.
- WhatsApp Web.js QR pairing, session supervision and account-policy acceptance; the private multi-account bridge and lifecycle controls are implemented and opt-in, but provider policy and live-account acceptance remain external.
- Live customer self-service settlement through Stripe credentials/webhook delivery and realtime deployment rollout remain external acceptance gates; the repository-side Stripe integration is delivered under ISP-033B.
- Whish Pay still requires live sandbox/merchant acceptance: verify the current official endpoint contract, callback field names, credentials, callback delivery and QR collection behavior before enabling production. The referenced community client was audited for the `/payment/whish`, `/payment/collect/status`, `externalId`, `currency`, `collectUrl`, and legacy `whishUrl` shapes; it remains a contract reference only, and its Node package is not shipped in this Laravel application.
- External monitoring destination, escalation policy and dashboard signal delivery; the repository-side signed, deduplicated platform health monitor is implemented and scheduled.
- Security operations: deployment-shaped PostgreSQL/object-media backup restore rehearsal, off-site retention and Sentry project validation; the local encrypted SQLite rehearsal passed.
- PostgreSQL 17 production-shaped pool and infrastructure-level cold-cache acceptance against the 50k-service seed; the isolated PostgreSQL 17 data/query-plan and post-restart evidence is recorded in the performance runbook.

## Deliberately not claimed yet

Production credentials, provider approvals, RouterOS CHR lab acceptance, native mobile offline storage/sync integration and pilot readiness remain external gates. The repository contains tested API seams and fakes for these boundaries; it does not claim those external integrations are production-ready.
