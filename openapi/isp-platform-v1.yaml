openapi: 3.1.0
info:
  title: ISP Manager API
  version: 1.0.0
description: Versioned mobile and portal contract for tenant-scoped reads, scoped Sanctum abilities, idempotent collection writes, offline collector sync, imports, technician operations and customer self-service.
servers:
  - url: /api/v1
security:
  - bearerAuth: []
paths:
  /health:
    get:
      security: []
      summary: Readiness check
      responses:
        '200':
          description: Application and dependencies are ready
        '503':
          description: One or more dependencies are degraded
  /tokens:
    post:
      security: []
      summary: Issue a scoped Sanctum API token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, device_name]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password }
                device_name: { type: string, maxLength: 100 }
                abilities:
                  type: array
                  items: { type: string, enum: [customer, staff:collector, staff:technician, staff:operator] }
                  description: Optional narrowing of the role-derived ability.
                otp: { type: string, minLength: 6, maxLength: 16 }
      responses:
        '200': { description: Bearer token and granted abilities }
        '401': { description: Invalid credentials }
        '403': { description: Requested ability is outside the user's role }
        '423': { description: Two-factor verification required }
  /tokens/current:
    delete:
      summary: Revoke the current API token
      responses:
        '204': { description: Token revoked }
        '401': { description: Unauthenticated }
  /auth/staff/login:
    post:
      security: []
      summary: Authenticate a staff device and issue a scoped Sanctum token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, device_name, device_id]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password }
                device_name: { type: string, maxLength: 100 }
                device_id: { type: string, maxLength: 100 }
                abilities:
                  type: array
                  items: { type: string, enum: [staff:collector, staff:technician, staff:operator] }
      responses:
        '200': { description: Bearer token, user identity and granted abilities }
        '401': { description: Invalid staff credentials }
        '422': { description: Two-factor challenge required; response includes challenge_id }
        '423': { description: Staff two-factor setup is required }
  /auth/staff/two-factor:
    post:
      security: []
      summary: Complete a staff two-factor authentication challenge
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [challenge_id, code]
              properties:
                challenge_id: { type: string, maxLength: 26 }
                code: { type: string, minLength: 6, maxLength: 16 }
      responses:
        '200': { description: Bearer token, user identity and granted abilities }
        '422': { description: Invalid or expired challenge/code }
  /auth/customer/otp/request:
    post:
      security: []
      summary: Request a customer OTP without revealing account existence
      description: Resolves the active customer tenant from the branded host, X-Tenant-Slug in local development, or the only active tenant.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [phone]
              properties:
                phone: { type: string, maxLength: 40 }
      responses:
        '200': { description: Generic OTP timing response for known and unknown phone numbers }
        '400': { description: Tenant context is missing when more than one tenant is active }
        '429': { description: Rate limited }
  /auth/customer/otp/verify:
    post:
      security: []
      summary: Verify a customer phone OTP and issue a portal bearer token
      parameters:
        - in: header
          name: X-Tenant-Slug
          required: false
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [phone, code, device_id]
              properties:
                phone: { type: string, maxLength: 40 }
                code: { type: string, pattern: '^[0-9]{6}$' }
                device_id: { type: string, maxLength: 100 }
      responses:
        '200': { description: Portal bearer token and expiry }
        '400': { description: Tenant context is missing when more than one tenant is active }
        '422': { description: Invalid or expired code }
  /auth/customer/logout:
    post:
      summary: Revoke the current customer portal session
      responses:
        '204': { description: Portal session revoked }
        '401': { description: Invalid portal session }
  /app/config:
    get:
      summary: Read mobile version and maintenance configuration
      responses:
        '200':
          description: Client launch configuration
          content:
            application/json:
              schema:
                type: object
                required: [min_supported_version, latest_version, maintenance, force_update]
                properties:
                  min_supported_version: { type: string }
                  latest_version: { type: string }
                  maintenance: { type: boolean }
                  message: { type: string, nullable: true }
                  force_update: { type: boolean }
  /plans:
    get:
      summary: List tenant plans with current effective prices
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string, enum: [active, inactive] }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated plan list }
        '403': { description: Missing plan management capability }
    post:
      summary: Create a tenant plan and its first effective price
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, download_kbps, upload_kbps, duration_days, amount_minor, currency, effective_from, status]
              properties:
                name: { type: string, maxLength: 255 }
                slug: { type: string, maxLength: 255, nullable: true }
                download_kbps: { type: integer, minimum: 0 }
                upload_kbps: { type: integer, minimum: 0 }
                duration_days: { type: integer, minimum: 1 }
                amount_minor: { type: integer, minimum: 0 }
                currency: { type: string, minLength: 3, maxLength: 3 }
                effective_from: { type: string, format: date }
                status: { type: string, enum: [active, inactive] }
      responses:
        '201': { description: Created plan with current price }
        '403': { description: Missing plan management capability }
        '422': { description: Validation failure or duplicate slug }
  /plans/{plan}:
    get:
      summary: Read one tenant plan with its current effective price
      parameters:
        - in: path
          name: plan
          required: true
          schema: { type: string }
      responses:
        '200': { description: Plan resource }
        '403': { description: Missing plan management capability }
        '404': { description: Plan not found }
  /invoices:
    get:
      summary: List tenant invoices with balances and line items
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string, enum: [draft, issued, void] }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: sort
          schema: { type: string, enum: [created_at, -created_at, issued_at, -issued_at, due_at, -due_at, total_amount, -total_amount], default: -created_at }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated invoice resources }
        '401': { description: Unauthenticated }
        '403': { description: Missing invoice view capability }
  /invoices/{invoice}:
    get:
      summary: Read one tenant invoice with payments and credit notes
      parameters:
        - in: path
          name: invoice
          required: true
          schema: { type: string, description: Invoice public ULID }
      responses:
        '200': { description: Invoice resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing invoice view capability }
        '404': { description: Invoice not found }
  /tickets:
    get:
      summary: List tenant support tickets
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string, enum: [open, in_progress, pending, resolved, closed] }
        - in: query
          name: filter[priority]
          schema: { type: string, enum: [critical, high, normal, low] }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated support tickets }
        '401': { description: Unauthenticated }
        '403': { description: Missing ticket view capability }
    post:
      summary: Create a staff support ticket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [customer_id, subject, description, category, priority]
              properties:
                customer_id: { type: string, description: Customer public ULID }
                service_id: { type: string, description: Service public ULID, nullable: true }
                subject: { type: string, maxLength: 160 }
                description: { type: string, maxLength: 10000 }
                category: { type: string, maxLength: 64 }
                priority: { type: string, enum: [critical, high, normal, low] }
      responses:
        '201': { description: Created support ticket }
        '401': { description: Unauthenticated }
        '403': { description: Missing ticket creation capability }
        '422': { description: Validation failure }
  /tickets/{ticket}:
    get:
      summary: Read one tenant support ticket with messages
      parameters:
        - in: path
          name: ticket
          required: true
          schema: { type: string, description: Ticket public ULID }
      responses:
        '200': { description: Support ticket resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing ticket view capability }
        '404': { description: Ticket not found }
  /tickets/{ticket}/messages:
    post:
      summary: Add a staff reply to a support ticket
      parameters:
        - in: path
          name: ticket
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body: { type: string, maxLength: 10000 }
                visibility: { type: string, enum: [public, internal], default: public }
      responses:
        '200': { description: Updated support ticket }
        '403': { description: Missing ticket reply capability }
        '404': { description: Ticket not found }
        '422': { description: Closed ticket or validation failure }
  /tickets/{ticket}/status:
    post:
      summary: Change a support ticket status
      parameters:
        - in: path
          name: ticket
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [open, in_progress, pending, resolved, closed] }
      responses:
        '200': { description: Updated support ticket }
        '403': { description: Missing ticket transition or close capability }
        '404': { description: Ticket not found }
        '422': { description: Invalid transition or validation failure }
  /routers:
    get:
      summary: List tenant network routers
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: sort
          schema: { type: string, enum: [name, -name, status, -status, last_seen_at, -last_seen_at], default: name }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated sanitized router resources }
        '401': { description: Unauthenticated }
        '403': { description: Missing network view capability }
    post:
      summary: Register a tenant network router
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, host, api_port, username, password, coa_port]
              properties:
                name: { type: string, maxLength: 120 }
                host: { type: string, maxLength: 255 }
                api_port: { type: integer, minimum: 1, maximum: 65535 }
                username: { type: string, maxLength: 128 }
                password: { type: string, format: password, minLength: 12, maxLength: 512 }
                radius_secret: { type: string, maxLength: 512, nullable: true }
                coa_port: { type: integer, minimum: 1, maximum: 65535 }
                tls_verify: { type: boolean, default: false }
                pop_id: { type: integer, nullable: true }
      responses:
        '201': { description: Created sanitized router resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing network provisioning capability }
        '422': { description: Validation failure }
  /routers/{router}:
    get:
      summary: Read one tenant network router
      parameters:
        - in: path
          name: router
          required: true
          schema: { type: string, description: Router public ULID }
      responses:
        '200': { description: Sanitized router resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing network view capability }
        '404': { description: Router not found }
  /work-orders:
    get:
      summary: List tenant work orders for operators
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string }
        - in: query
          name: filter[type]
          schema: { type: string }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated work orders }
        '401': { description: Unauthenticated }
        '403': { description: Missing work-order capability }
  /work-orders/{workOrder}:
    get:
      summary: Read one tenant work order with execution history
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string, description: Work-order public ULID }
      responses:
        '200': { description: Work-order resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing work-order capability }
        '404': { description: Work order not found }
  /zones:
    get:
      summary: List tenant service zones
      parameters:
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: sort
          schema: { type: string, enum: [name, -name, code, -code, created_at, -created_at], default: name }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated zone resources }
        '401': { description: Unauthenticated }
        '403': { description: Missing customer view capability }
  /zones/{zone}:
    get:
      summary: Read one tenant service zone
      parameters:
        - in: path
          name: zone
          required: true
          schema: { type: integer }
      responses:
        '200': { description: Zone resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing customer view capability }
        '404': { description: Zone not found }
  /me:
    get:
      summary: Read the authenticated staff identity
      responses:
        '200': { description: Current user and role data }
        '401': { description: Unauthenticated }
  /me/profile:
    get:
      summary: Read the authenticated customer profile
      responses:
        '200': { description: Customer profile and services }
        '401': { description: Invalid portal session }
    patch:
      summary: Update the authenticated customer profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email: { type: string, format: email, nullable: true }
                address: { type: string, maxLength: 500, nullable: true }
                notification_preferences: { type: object, additionalProperties: true }
      responses:
        '200': { description: Updated customer profile fields }
        '401': { description: Invalid portal session }
        '422': { description: Validation failure }
  /me/services:
    get:
      summary: List the authenticated customer's services
      responses:
        '200': { description: Customer service summaries }
        '401': { description: Invalid portal session }
  /me/services/{service}/usage:
    get:
      summary: Read daily usage for an owned customer service
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: query
          name: from
          schema: { type: string, format: date }
        - in: query
          name: to
          schema: { type: string, format: date }
      responses:
        '200': { description: Daily input, output and total octets }
        '401': { description: Invalid portal session }
        '404': { description: Service is not owned by the customer }
  /me/services/{service}/restart-session:
    post:
      summary: Queue a customer-owned PPP session restart
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Network disconnect queued }
        '401': { description: Invalid portal session }
        '404': { description: Service is not owned by the customer }
        '429': { description: Restart limited to once every five minutes }
  /me/notices:
    get:
      summary: Read active customer-scoped outage notices
      responses:
        '200': { description: Active outage notices }
        '401': { description: Invalid portal session }
  /me/tickets:
    get:
      summary: List the authenticated customer's support tickets
      responses:
        '200': { description: Customer-owned ticket summaries }
        '401': { description: Invalid portal session }
    post:
      summary: Open a support ticket for the authenticated customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category, subject, description]
              properties:
                category: { type: string, enum: [no_service, slow, billing, relocation, other] }
                subject: { type: string, maxLength: 160 }
                description: { type: string, maxLength: 5000 }
                service_uuid: { type: string, nullable: true }
      responses:
        '201': { description: Ticket created }
        '401': { description: Invalid portal session }
        '422': { description: Validation failure }
  /me/tickets/{ticket}:
    get:
      summary: Read a customer-owned ticket and public messages
      parameters:
        - in: path
          name: ticket
          required: true
          schema: { type: string }
      responses:
        '200': { description: Ticket detail and public message history }
        '401': { description: Invalid portal session }
        '404': { description: Ticket not found }
  /me/tickets/{ticket}/messages:
    post:
      summary: Add a public reply to a customer-owned ticket
      parameters:
        - in: path
          name: ticket
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body: { type: string, maxLength: 5000 }
      responses:
        '200': { description: Ticket with the appended public message }
        '401': { description: Invalid portal session }
        '404': { description: Ticket not found }
  /me/tickets/{ticket}/rating:
    post:
      summary: Rate a resolved or closed customer-owned ticket
      parameters:
        - in: path
          name: ticket
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating: { type: integer, minimum: 1, maximum: 5 }
      responses:
        '200': { description: Ticket with the saved satisfaction rating }
        '401': { description: Invalid portal session }
        '404': { description: Ticket not found }
        '422': { description: Ticket is not resolved or rating is invalid }
  /me/balance:
    get:
      summary: Read the authenticated customer's balance
      responses:
        '200': { description: Customer balance and next outstanding invoice }
        '401': { description: Invalid portal session }
  /me/invoices:
    get:
      summary: List the authenticated customer's invoices
      parameters:
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated invoice summaries }
        '401': { description: Invalid portal session }
  /me/invoices/{invoice}:
    get:
      summary: Read one authenticated customer invoice
      parameters:
        - in: path
          name: invoice
          required: true
          schema: { type: string }
      responses:
        '200': { description: Invoice detail with lines and allocated payments }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
  /me/invoices/{invoice}/pdf:
    get:
      summary: Download one authenticated customer invoice as PDF
      parameters:
        - in: path
          name: invoice
          required: true
          schema: { type: string }
      responses:
        '200': { description: A4 invoice PDF }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
  /me/payments:
    get:
      summary: List the authenticated customer's posted payments
      parameters:
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated payment receipt summaries }
        '401': { description: Invalid portal session }
  /me/payments/intent:
    post:
      summary: Create a customer payment intent through the configured gateway
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [invoice_id, amount]
              properties:
                invoice_id: { type: string }
                amount: { type: integer, minimum: 1 }
      responses:
        '201': { description: Gateway-specific payment intent payload }
        '400': { description: Missing idempotency key }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
        '503': { description: No payment gateway is configured }
  /auth/me:
    get:
      summary: Read the authenticated staff identity and token abilities
      responses:
        '200': { description: Current user and token ability data }
        '401': { description: Unauthenticated }
  /auth/logout:
    post:
      summary: Revoke the current bearer token
      responses:
        '204': { description: Token revoked }
        '401': { description: Unauthenticated }
  /auth/devices/{device}/revoke:
    post:
      summary: Revoke all bearer tokens registered to a device
      parameters:
        - in: path
          name: device
          required: true
          schema: { type: string, maxLength: 100 }
      responses:
        '204': { description: Device tokens revoked }
        '401': { description: Unauthenticated }
  /auth/push-token:
    post:
      summary: Register or refresh an encrypted push notification token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, platform, app]
              properties:
                token: { type: string, maxLength: 4096 }
                platform: { type: string, enum: [android, ios, web] }
                app: { type: string, maxLength: 100 }
      responses:
        '204': { description: Push token registered }
        '401': { description: Unauthenticated }
  /customers:
    get:
      summary: List tenant customers
      parameters:
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: filter[status]
          schema: { type: string, enum: [active, inactive, archived] }
        - in: query
          name: sort
          schema: { type: string, enum: [created_at, -created_at, code, -code], default: -created_at }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Paginated customer list }
        '401': { description: Unauthenticated }
        '403': { description: Forbidden }
    post:
      summary: Create a tenant customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [first_name, phone]
              properties:
                first_name: { type: string, maxLength: 100 }
                last_name: { type: string, maxLength: 100, nullable: true }
                phone: { type: string, maxLength: 32 }
                email: { type: string, format: email, maxLength: 255, nullable: true }
                zone_id: { type: integer, nullable: true }
                address: { type: string, maxLength: 500, nullable: true }
                latitude: { type: number, minimum: -90, maximum: 90, nullable: true }
                longitude: { type: number, minimum: -180, maximum: 180, nullable: true }
                create_service: { type: boolean, default: false }
                plan_id: { type: integer, nullable: true }
                username: { type: string, maxLength: 64, nullable: true }
                password: { type: string, format: password, minLength: 12, maxLength: 128, nullable: true }
                provisioning_mode: { type: string, enum: [manual, upstream_credential, mikrotik, radius, external], nullable: true }
                router_id: { type: integer, nullable: true }
      responses:
        '201': { description: Created customer resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing customer or service creation capability }
        '422': { description: Validation failure }
  /customers/{customer}:
    get:
      summary: Read one tenant customer
      parameters:
        - in: path
          name: customer
          required: true
          schema: { type: string, description: Customer public ULID }
      responses:
        '200': { description: Customer resource }
        '401': { description: Unauthenticated }
        '404': { description: Customer not found }
    patch:
      summary: Partially update a tenant customer
      parameters:
        - in: path
          name: customer
          required: true
          schema: { type: string, description: Customer public ULID }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                first_name: { type: string, maxLength: 100 }
                last_name: { type: string, maxLength: 100, nullable: true }
                phone: { type: string, maxLength: 32 }
                email: { type: string, format: email, maxLength: 255, nullable: true }
                zone_id: { type: integer, nullable: true }
                address: { type: string, maxLength: 500, nullable: true }
                latitude: { type: number, minimum: -90, maximum: 90, nullable: true }
                longitude: { type: number, minimum: -180, maximum: 180, nullable: true }
      responses:
        '200': { description: Updated customer resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing customer update capability }
        '404': { description: Customer not found }
        '422': { description: Validation failure }
  /services:
    get:
      summary: List tenant services
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string, enum: [pending, active, suspended, paused, terminated] }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated service list }
        '401': { description: Unauthenticated }
        '403': { description: Missing staff ability }
    post:
      summary: Create a pending tenant service
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [customer_id, plan_id, username, password, provisioning_mode]
              properties:
                customer_id: { type: string, description: Customer public ULID }
                plan_id: { type: string, description: Active plan public ULID }
                username: { type: string, maxLength: 64 }
                password: { type: string, format: password, minLength: 12, maxLength: 128 }
                provisioning_mode: { type: string, enum: [manual, upstream_credential, mikrotik, radius, external] }
                router_id: { type: string, description: Router public ULID, nullable: true }
                billing_anchor_day: { type: integer, minimum: 1, maximum: 31, nullable: true, description: Optional monthly renewal day; short months clamp to their final day }
      responses:
        '201': { description: Created pending service resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing service creation capability }
        '422': { description: Validation failure }
  /services/{service}:
    get:
      summary: Read one tenant service with plan, customer and network history
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
      responses:
        '200': { description: Service resource }
        '403': { description: Missing service view capability }
        '404': { description: Service not found }
  /services/{service}/network-commands:
    get:
      summary: List recent network commands for a service
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 50, default: 20 }
      responses:
        '200': { description: Sanitized recent command history }
        '403': { description: Missing service view capability }
        '404': { description: Service not found }
  /network-commands/{command}:
    get:
      summary: Poll a network command
      parameters:
        - in: path
          name: command
          required: true
          schema: { type: string }
      responses:
        '200': { description: Sanitized command status and result }
        '403': { description: Missing network view capability }
        '404': { description: Network command not found }
  /services/{service}/activate:
    post:
      summary: Activate a service and enqueue network provisioning
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Commercial state changed and network command queued }
        '403': { description: Missing activation capability }
  /services/{service}/suspend:
    post:
      summary: Suspend a service and enqueue network enforcement
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, maxLength: 64 }
                note: { type: string, maxLength: 5000 }
      responses:
        '202': { description: Commercial state changed and network command queued }
        '403': { description: Missing suspension capability }
  /services/{service}/resume:
    post:
      summary: Resume a suspended service and enqueue network enforcement
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Commercial state changed and network command queued }
        '403': { description: Missing activation or force-resume capability }
  /services/{service}/pause:
    post:
      summary: Pause an active service while preserving its billing expiry
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, maxLength: 64 }
                resume_at: { type: string, format: date-time, nullable: true }
      responses:
        '202': { description: Service paused and network enforcement queued }
        '403': { description: Missing pause capability }
        '422': { description: Only active services can be paused }
  /services/{service}/terminate:
    post:
      summary: Terminate a service and enqueue network disconnect
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, maxLength: 5000 }
      responses:
        '202': { description: Service terminated, assigned equipment returned to recovery custody, and network disconnect queued }
        '403': { description: Missing termination capability }
  /services/{service}/disconnect-session:
    post:
      summary: Disconnect the current live session for a service
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Network disconnect queued with the latest live session identifier when available }
        '403': { description: Missing network disconnect capability }
  /services/{service}/resync:
    post:
      summary: Re-push the service's desired state to its network driver
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Desired service state queued for network synchronization }
        '403': { description: Missing service state capability }
        '422': { description: Terminated services cannot be re-synced }
  /services/{service}/plan-change-previews:
    post:
      summary: Preview an immediate or next-cycle service plan change
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlanChangeRequest'
      responses:
        '200': { description: Proration amounts and effective date preview }
        '403': { description: Missing service plan-change capability }
        '422': { description: Plan change cannot be applied }
  /services/{service}/change-plan:
    post:
      summary: Change a service plan and queue network synchronization
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlanChangeRequest'
      responses:
        '202': { description: Plan changed or scheduled and network synchronization queued when applicable }
        '403': { description: Missing service plan-change capability }
        '409': { description: Idempotency key reused for a different request }
        '422': { description: Plan change cannot be applied }
  /services/{service}/renewal-previews:
    post:
      summary: Preview a service renewal before issuing its invoice
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string, description: Service public ULID }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                periods: { type: integer, minimum: 1, maximum: 12, default: 1 }
      responses:
        '200': { description: Signed short-lived preview with amount and new expiry }
        '403': { description: Missing service view capability }
        '422': { description: Renewal cannot be previewed }
  /services/{service}/renewals:
    post:
      summary: Issue an invoice for a previously previewed service renewal
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string, description: Service public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RenewalRequest'
      responses:
        '202': { description: Renewal invoice issued; service is extended after full payment }
        '403': { description: Missing service view capability }
        '409': { description: Idempotency key reused for a different request }
        '422': { description: Preview is invalid or expired }
  /payments:
    get:
      summary: List tenant payments with customer and invoice references
      parameters:
        - in: query
          name: filter[status]
          schema: { type: string, enum: [posted, reversed] }
        - in: query
          name: filter[method]
          schema: { type: string, enum: [cash, bank_transfer, card, mobile_wallet] }
        - in: query
          name: filter[search]
          schema: { type: string }
        - in: query
          name: sort
          schema: { type: string, enum: [received_at, -received_at, amount, -amount, created_at, -created_at], default: -received_at }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated payment resources }
        '401': { description: Unauthenticated }
        '403': { description: Missing payment collection capability }
    post:
      summary: Record a customer payment
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
      responses:
        '201': { description: Payment recorded }
        '400': { description: Missing or invalid idempotency key }
        '409': { description: Idempotency key reused for a different request }
        '422': { description: Validation failed }
  /payments/{payment}:
    get:
      summary: Read one tenant payment with allocations
      parameters:
        - in: path
          name: payment
          required: true
          schema: { type: string, description: Payment public ULID }
      responses:
        '200': { description: Payment resource }
        '401': { description: Unauthenticated }
        '403': { description: Missing payment collection capability }
        '404': { description: Payment not found }
  /portal/{tenant}/otp/request:
    post:
      security: []
      summary: Request a customer portal OTP
      description: Always returns the same response shape for known and unknown phone numbers.
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [phone]
              properties:
                phone: { type: string }
      responses:
        '200': { description: OTP challenge created }
        '429': { description: Rate limited }
  /portal/{tenant}/otp/verify:
    post:
      security: []
      summary: Verify a customer portal OTP
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Portal bearer session issued }
        '422': { description: Invalid or expired code }
  /portal/{tenant}/logout:
    post:
      summary: Revoke the current tenant-scoped customer portal session
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '204': { description: Portal session revoked }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me:
    get:
      summary: Read the authenticated customer portal profile
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Customer profile and services }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me/profile:
    get:
      summary: Read the authenticated customer portal profile
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Customer profile and services }
        '401': { description: Invalid portal session }
    patch:
      summary: Update customer contact and notification preferences
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email: { type: string, format: email, nullable: true }
                address: { type: string, maxLength: 500, nullable: true }
                notification_preferences: { type: object, additionalProperties: true }
      responses:
        '200': { description: Updated customer profile fields }
        '401': { description: Invalid portal session }
        '422': { description: Validation failed }
  /portal/{tenant}/me/balance:
    get:
      summary: Read the authenticated customer's balance and next due invoice
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Customer balance and next outstanding invoice }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me/invoices:
    get:
      summary: List the authenticated customer's invoices
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated invoice summaries with line items }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me/invoices/{invoice}:
    get:
      summary: Read one authenticated customer invoice
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: invoice
          required: true
          schema: { type: string, description: Invoice public ULID }
      responses:
        '200': { description: Invoice detail with lines and allocated payments }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
  /portal/{tenant}/me/invoices/{invoice}/pdf:
    get:
      summary: Download one authenticated customer invoice as PDF
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: invoice
          required: true
          schema: { type: string, description: Invoice public ULID }
      responses:
        '200':
          description: A4 invoice PDF
          content:
            application/pdf: { schema: { type: string, format: binary } }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
  /portal/{tenant}/me/payments:
    get:
      summary: List the authenticated customer's posted payments
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - in: query
          name: cursor
          schema: { type: string }
      responses:
        '200': { description: Cursor-paginated payment receipt summaries }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me/services:
    get:
      summary: List the authenticated customer's services and current sessions
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Customer service summaries }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me/payments/intent:
    post:
      summary: Create a customer payment intent through the configured gateway
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '201': { description: Gateway-specific payment intent payload }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
        '503': { description: No payment gateway is configured }
  /portal/{tenant}/me/services/{service}/usage:
    get:
      summary: Read daily usage for an owned service
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: service
          required: true
          schema: { type: string, description: Service public ULID }
        - in: query
          name: from
          schema: { type: string, format: date }
        - in: query
          name: to
          schema: { type: string, format: date }
      responses:
        '200': { description: Daily input, output and total octets }
        '401': { description: Invalid portal session }
        '404': { description: Service is not owned by the portal customer }
  /portal/{tenant}/me/services/{service}/restart-session:
    post:
      summary: Queue a customer-owned PPP session restart
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: service
          required: true
          schema: { type: string, description: Service public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Network disconnect queued for the customer-owned service }
        '401': { description: Invalid portal session }
        '404': { description: Service is not owned by the portal customer }
        '429': { description: Restart limited to once every five minutes }
  /portal/{tenant}/me/notices:
    get:
      summary: Read active incidents affecting the customer's zone or POP
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Active customer-scoped outage notices }
        '401': { description: Invalid portal session }
  /portal/{tenant}/me/tickets:
    get:
      summary: List the authenticated customer's support tickets
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Customer-owned ticket summaries }
        '401': { description: Invalid portal session }
    post:
      summary: Open a support ticket for the authenticated customer
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category, subject, description]
              properties:
                category: { type: string, enum: [no_service, slow, billing, relocation, other] }
                subject: { type: string, maxLength: 160 }
                description: { type: string, maxLength: 5000 }
                service_uuid: { type: string, nullable: true }
      responses:
        '201': { description: Ticket created }
        '401': { description: Invalid portal session }
        '422': { description: Validation failed }
  /portal/{tenant}/me/tickets/{ticket}:
    get:
      summary: Read a customer-owned ticket and public messages
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: ticket
          required: true
          schema: { type: string, description: Ticket public ULID }
      responses:
        '200': { description: Ticket detail and public message history }
        '401': { description: Invalid portal session }
        '404': { description: Ticket not found }
  /portal/{tenant}/me/tickets/{ticket}/messages:
    post:
      summary: Add a public reply to a customer-owned ticket
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: ticket
          required: true
          schema: { type: string, description: Ticket public ULID }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body: { type: string, maxLength: 5000 }
      responses:
        '200': { description: Ticket with the appended public message }
        '401': { description: Invalid portal session }
        '404': { description: Ticket not found }
  /portal/{tenant}/me/tickets/{ticket}/rating:
    post:
      summary: Rate a resolved or closed customer-owned ticket
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: path
          name: ticket
          required: true
          schema: { type: string, description: Ticket public ULID }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating: { type: integer, minimum: 1, maximum: 5 }
      responses:
        '200': { description: Ticket with the saved satisfaction rating }
        '401': { description: Invalid portal session }
        '404': { description: Ticket not found }
        '422': { description: Ticket is not resolved or rating is invalid }
  /portal/{tenant}/billing:
    get:
      summary: Read the authenticated customer's invoice and payment history
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
      responses:
        '200': { description: Recent invoices, lines and payments }
        '401': { description: Invalid portal session }
  /portal/{tenant}/payments/intent:
    post:
      summary: Create a customer payment intent through the configured gateway
      parameters:
        - in: path
          name: tenant
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [invoice_id, amount]
              properties:
                invoice_id: { type: string, description: Invoice public ULID }
                amount: { type: integer, minimum: 1, description: Amount in minor currency units }
      responses:
        '200': { description: Gateway-specific payment intent payload }
        '401': { description: Invalid portal session }
        '404': { description: Invoice not found for this customer }
        '503': { description: No payment gateway is configured }
  /public-billing/{token}/stripe-intent:
    post:
      security: []
      summary: Create a Stripe intent from an active public invoice payment link
      parameters:
        - in: path
          name: token
          required: true
          schema: { type: string, minLength: 64, maxLength: 64 }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                amount: { type: integer, minimum: 1, description: Amount in invoice minor currency units }
      responses:
        '201': { description: Stripe intent and browser-safe confirmation payload }
        '404': { description: Link is invalid, expired, revoked, or not payment-enabled }
        '422': { description: Missing idempotency key or amount exceeds the invoice balance }
  /public-billing/{token}/whish:
    post:
      security: []
      summary: Create a Whish QR attempt from an active public invoice payment link
      parameters:
        - in: path
          name: token
          required: true
          schema: { type: string, minLength: 64, maxLength: 64 }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                amount: { type: integer, minimum: 1, description: Amount in invoice minor currency units }
      responses:
        '201': { description: Pending Whish attempt, collection URL and QR data URI }
        '404': { description: Link is invalid, expired, revoked, or not payment-enabled }
        '422': { description: Missing idempotency key, unsupported currency, or amount exceeds the invoice balance }
        '502': { description: Whish did not return a usable collection URL }
  /public-billing/{token}/whish/{attempt}:
    get:
      security: []
      summary: Verify and poll a Whish attempt created from a public payment link
      parameters:
        - in: path
          name: token
          required: true
          schema: { type: string, minLength: 64, maxLength: 64 }
        - in: path
          name: attempt
          required: true
          schema: { type: string, description: Payment attempt public ULID }
      responses:
        '200': { description: Verified terminal or pending Whish attempt status }
        '404': { description: Link or matching payment attempt not found }
        '422': { description: Whish status could not be verified or settled }
  /collector/payments/batch:
    post:
      summary: Record collector payments in a partial-success batch
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [items]
              properties:
                items:
                  type: array
                  maxItems: 100
                  items: { $ref: '#/components/schemas/PaymentRequest' }
      responses:
        '200': { description: Per-item success/error results }
        '409': { description: Idempotency key reused for a different request }
  /collector/payments/whish:
    post:
      summary: Create a collector Whish payment attempt and QR payload
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [customer_id, amount, currency]
              properties:
                customer_id: { type: string, description: Customer public ULID }
                invoice_id: { type: string, nullable: true, description: Invoice public ULID }
                amount: { type: integer, minimum: 1, description: Amount in minor currency units }
                currency: { type: string, enum: [USD, LBP, AED] }
      responses:
        '201': { description: Pending Whish collect URL and SVG QR data URI }
        '403': { description: Missing collector ability }
        '422': { description: Invalid customer, invoice, currency, or Whish configuration }
        '502': { description: Whish did not return a usable collection URL }
  /collector/payments/whish/{attempt}:
    get:
      summary: Poll and verify a collector Whish payment attempt
      parameters:
        - in: path
          name: attempt
          required: true
          schema: { type: string, description: Payment attempt public ULID }
      responses:
        '200': { description: Verified terminal or pending Whish attempt status }
        '403': { description: Missing collector ability }
        '404': { description: Payment attempt not found }
        '502': { description: Whish status could not be verified or posted }
  /collector/shift:
    get:
      summary: Read the authenticated collector's open cash shift
      responses:
        '200': { description: Open shift, opening float, running totals and payment count; data is null when no shift is open }
        '403': { description: Missing collector ability }
  /collector/customers:
    get:
      summary: List collector customers by search, zone, due state, or overdue state
      parameters:
        - in: query
          name: q
          schema: { type: string, maxLength: 120 }
        - in: query
          name: zone
          schema: { type: string, maxLength: 100 }
        - in: query
          name: status
          schema: { type: string, example: due,overdue }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        '200': { description: Cursor-paginated collector-safe customer summaries }
        '403': { description: Missing collector ability }
  /collector/customers/{customer}:
    get:
      summary: Read one collector customer with service, balance, location, and last payment
      parameters:
        - in: path
          name: customer
          required: true
          schema: { type: string, description: Customer public ULID }
      responses:
        '200': { description: Collector-safe customer detail }
        '403': { description: Missing customer view capability }
        '404': { description: Customer not found }
  /collector/shift/open:
    post:
      summary: Open the authenticated collector's cash shift
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                opening_float:
                  type: object
                  additionalProperties: { type: integer, minimum: 0 }
                  description: Opening cash by ISO currency in minor units
      responses:
        '201': { description: Shift opened }
        '409': { description: Idempotency key reused for a different request }
        '422': { description: Collector already has an open shift or the opening float is invalid }
  /collector/shift/close:
    post:
      summary: Close and reconcile the authenticated collector's cash shift
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [declared_totals]
              properties:
                declared_totals:
                  type: object
                  additionalProperties: { type: integer, minimum: 0 }
                variance_note: { type: string, maxLength: 2000, nullable: true }
      responses:
        '200': { description: Closed shift with normalized system and declared totals }
        '409': { description: No open shift or idempotency key conflict }
        '422': { description: Variance note missing or totals invalid }
  /collector/payments:
    get:
      summary: List the authenticated collector's posted payments
      parameters:
        - in: query
          name: date
          schema: { type: string, format: date }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        '200': { description: Cursor-paginated collector receipts and allocations }
        '403': { description: Missing collector ability }
    post:
      summary: Record collector payments in a partial-success batch
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [items]
              properties:
                items:
                  type: array
                  maxItems: 100
                  items: { $ref: '#/components/schemas/PaymentRequest' }
      responses:
        '200': { description: Per-item success/error results }
        '409': { description: Idempotency key reused for a different request }
  /collector/summary:
    get:
      summary: Read the authenticated collector's daily payment totals
      parameters:
        - in: query
          name: date
          schema: { type: string, format: date }
      responses:
        '200': { description: Payment count and totals by currency }
        '403': { description: Missing collector ability }
  /collector/payments/{payment}/receipt:
    post:
      summary: Queue a payment receipt for resend through one channel
      parameters:
        - in: path
          name: payment
          required: true
          schema: { type: string, description: Payment public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [channel]
              properties:
                channel: { type: string, enum: [whatsapp, sms, email] }
      responses:
        '202': { description: Receipt queued or not queued because the channel is unavailable }
        '403': { description: Payment is not owned by the collector }
        '422': { description: Receipt cannot be resent }
  /collector/sync/bootstrap:
    get:
      summary: Bootstrap the collector's offline cache
      parameters:
        - in: query
          name: zone
          schema: { type: string, maxLength: 100 }
          description: Optional zone code used to narrow customer and service rows.
      responses:
        '200': { description: Full customer, service, plan and message snapshot with a signed sync token }
        '403': { description: Missing collector ability }
  /collector/sync/delta:
    get:
      summary: Read collector changes since a signed sync token
      parameters:
        - in: query
          name: since
          required: true
          schema: { type: string }
        - in: query
          name: zone
          schema: { type: string, maxLength: 100 }
      responses:
        '200': { description: Changed rows, tombstones and a replacement sync token }
        '403': { description: Missing collector ability }
        '422': { description: Invalid or foreign sync token }
  /collector/sync/push:
    post:
      summary: Push queued offline collector payments with partial success
      parameters:
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              minItems: 1
              maxItems: 100
              items: { $ref: '#/components/schemas/CollectorSyncPayment' }
      responses:
        '200': { description: Per-item created, replayed or rejected results }
        '422': { description: Invalid batch envelope }
        '409': { description: Idempotency key reused for a different request }
  /imports/customers:
    post:
      summary: Preview or import customers from CSV
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/ImportRequest' } }
          multipart/form-data: { schema: { $ref: '#/components/schemas/ImportRequest' } }
      responses:
        '200': { description: Dry-run validation report }
        '201': { description: Completed import report }
        '403': { description: Missing customer capability }
  /imports/plans:
    post:
      summary: Preview or import plans from CSV
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/ImportRequest' } }
          multipart/form-data: { schema: { $ref: '#/components/schemas/ImportRequest' } }
      responses:
        '200': { description: Dry-run validation report }
        '201': { description: Completed import report }
        '403': { description: Missing plan capability }
  /imports/services:
    post:
      summary: Preview or import services from CSV
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/ImportRequest' } }
          multipart/form-data: { schema: { $ref: '#/components/schemas/ImportRequest' } }
      responses:
        '200': { description: Dry-run validation report }
        '201': { description: Completed import report }
        '403': { description: Missing service capability }
  /imports/equipment:
    post:
      summary: Preview or import serialized equipment from CSV
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/ImportRequest' } }
          multipart/form-data: { schema: { $ref: '#/components/schemas/ImportRequest' } }
      responses:
        '200': { description: Dry-run validation report }
        '201': { description: Completed import report }
        '403': { description: Missing inventory capability }
  /imports/balances:
    post:
      summary: Preview or import signed opening balances from CSV
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/ImportRequest' } }
          multipart/form-data: { schema: { $ref: '#/components/schemas/ImportRequest' } }
      responses:
        '200': { description: Dry-run validation report }
        '201': { description: Completed journal-backed import report }
        '403': { description: Missing billing adjustment capability }
  /imports/{import}/rollback:
    post:
      summary: Roll back an imported customer batch
      parameters:
        - in: path
          name: import
          required: true
          schema: { type: string }
      responses:
        '200': { description: Rollback result }
        '403': { description: Missing customer capability }
        '409': { description: Import cannot be rolled back safely }
  /imports/plans/{import}/rollback:
    post:
      summary: Roll back an imported plan batch
      parameters:
        - in: path
          name: import
          required: true
          schema: { type: string }
      responses:
        '200': { description: Rollback result }
        '403': { description: Missing plan capability }
        '409': { description: Import cannot be rolled back safely }
  /imports/services/{import}/rollback:
    post:
      summary: Roll back an imported service batch
      parameters:
        - in: path
          name: import
          required: true
          schema: { type: string }
      responses:
        '200': { description: Rollback result }
        '403': { description: Missing service capability }
        '409': { description: Import cannot be rolled back safely }
  /imports/equipment/{import}/rollback:
    post:
      summary: Roll back an imported equipment batch
      parameters:
        - in: path
          name: import
          required: true
          schema: { type: string }
      responses:
        '200': { description: Rollback result }
        '403': { description: Missing inventory capability }
        '409': { description: Import cannot be rolled back safely }
  /imports/balances/{import}/rollback:
    post:
      summary: Roll back an imported balance batch
      parameters:
        - in: path
          name: import
          required: true
          schema: { type: string }
      responses:
        '200': { description: Reversing journal entries created for the batch }
        '403': { description: Missing billing adjustment capability }
        '409': { description: Import cannot be rolled back safely }
  /imports/routers/{router}/subscribers:
    post:
      summary: Discover RouterOS PPP subscribers for an import report
      parameters:
        - in: path
          name: router
          required: true
          schema: { type: string, description: Router public ULID }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                dry_run: { type: boolean, default: false }
      responses:
        '200': { description: Dry-run discovery report }
        '201': { description: Completed subscriber discovery import }
        '403': { description: Missing network view capability }
  /partners/{partner}/catalog:
    get:
      summary: Read the effective plan catalog for a visible partner
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
      responses:
        '200': { description: Partner-scoped sell prices and permitted commercial fields }
        '403': { description: Missing service view capability }
        '404': { description: Partner not found }
  /partners/{partner}/settlements:
    get:
      summary: List settlements for a visible partner
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
      responses:
        '200': { description: Partner settlement statements }
        '403': { description: Missing wallet view capability }
        '404': { description: Partner not found }
    post:
      summary: Generate a partner settlement statement
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [period_start, period_end, currency]
              properties:
                period_start: { type: string, format: date }
                period_end: { type: string, format: date }
                currency: { type: string, minLength: 3, maxLength: 3 }
      responses:
        '201': { description: Generated settlement statement }
        '403': { description: Missing settlement approval capability }
        '404': { description: Partner not found }
  /settlements/{settlement}/approve:
    post:
      summary: Approve a generated partner settlement
      parameters:
        - in: path
          name: settlement
          required: true
          schema: { type: string, description: Settlement public ULID }
      responses:
        '200': { description: Approved settlement statement }
        '403': { description: Missing settlement approval capability }
        '404': { description: Settlement not found or not visible }
  /settlements/{settlement}/pay:
    post:
      summary: Pay an approved partner settlement
      parameters:
        - in: path
          name: settlement
          required: true
          schema: { type: string, description: Settlement public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '200': { description: Paid settlement statement }
        '403': { description: Missing settlement approval capability }
        '404': { description: Settlement not found or not visible }
        '409': { description: Idempotency key reused for a different request }
  /partners:
    get:
      summary: List partners visible to the authenticated operator
      responses:
        '200': { description: Descendant-scoped partner wallets }
        '403': { description: Missing wallet view capability }
    post:
      summary: Create a visible partner account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, code, currency]
              properties:
                name: { type: string, maxLength: 255 }
                code: { type: string, maxLength: 32, pattern: '^[A-Za-z0-9_-]+$' }
                currency: { type: string, minLength: 3, maxLength: 3 }
                parent_id: { type: string, nullable: true, description: Visible parent partner public ULID }
                credit_limit: { type: integer, minimum: 0, default: 0 }
                low_balance_threshold: { type: integer, minimum: 0, default: 0 }
      responses:
        '201': { description: Created partner and empty wallet summary }
        '403': { description: Missing partner management capability }
        '422': { description: Invalid partner data or duplicate code }
  /partners/{partner}:
    get:
      summary: Read a visible partner and wallet statement
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
      responses:
        '200': { description: Partner wallet and transactions }
        '403': { description: Missing wallet view capability }
        '404': { description: Partner not found }
  /partners/{partner}/wallets:
    get:
      summary: Read the visible partner wallet balance and limits
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
      responses:
        '200': { description: Partner wallet balance, credit limit and threshold }
        '403': { description: Missing wallet view capability }
        '404': { description: Partner not found }
  /partners/{partner}/wallet-transactions:
    get:
      summary: List visible partner wallet transactions
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - in: query
          name: cursor
          schema: { type: string }
        - in: query
          name: type
          schema: { type: string, enum: [top_up, renewal] }
        - in: query
          name: direction
          schema: { type: string, enum: [credit, debit] }
      responses:
        '200': { description: Cursor-paginated partner wallet transactions }
        '403': { description: Missing wallet view capability }
        '404': { description: Partner not found }
  /partners/{partner}/wallet-top-ups:
    post:
      summary: Fund a partner wallet
      parameters:
        - in: path
          name: partner
          required: true
          schema: { type: string, description: Partner public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                amount: { type: integer, minimum: 1 }
      responses:
        '201': { description: Wallet transaction created }
        '401': { description: Recent authentication is required for wallet funding }
        '403': { description: Missing wallet funding capability }
        '409': { description: Idempotency key reused for a different request }
  /network/commands/{command}/retry:
    post:
      summary: Retry a failed network command
      parameters:
        - in: path
          name: command
          required: true
          schema: { type: string, description: Network command public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Replacement network command queued }
        '403': { description: Missing network provision capability }
  /provisioning-commands/{command}/retries:
    post:
      summary: Retry a failed provisioning command
      parameters:
        - in: path
          name: command
          required: true
          schema: { type: string, description: Network command public ULID }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '202': { description: Replacement network command queued }
        '403': { description: Missing network provision capability }
  /technician/uploads:
    post:
      summary: Upload a technician work-order image separately from completion
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary, description: JPEG, PNG or WebP image up to 20 MB }
      responses:
        '201': { description: Public media ID and non-sensitive file metadata }
        '403': { description: Missing work-order capability }
        '422': { description: Invalid media type or size }
  /technician/work-orders:
    get:
      summary: List work orders assigned to the authenticated technician
      parameters:
        - in: query
          name: date
          schema: { type: string, format: date }
        - in: query
          name: status
          schema: { type: string, enum: [pending, assigned, en_route, in_progress, completed, cancelled, failed] }
      responses:
        '200': { description: Assigned work-order summaries }
        '403': { description: Missing work-order capability }
  /technician/work-orders/{workOrder}:
    get:
      summary: Read an assigned work-order detail
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      responses:
        '200': { description: Work-order detail with checklist, event history, readings, consumed materials, signature metadata and safe media metadata }
        '403': { description: Missing work-order capability }
        '404': { description: Work order not found }
  /technician/work-orders/{workOrder}/media:
    post:
      summary: Upload evidence to an assigned work order
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary, description: JPEG, PNG or WebP image up to 20 MB }
                purpose: { type: string, enum: [evidence, other], default: evidence }
      responses:
        '201': { description: Public media ID and non-sensitive file metadata }
        '403': { description: Missing work-order capability }
        '404': { description: Work order is not assigned to the authenticated technician }
        '422': { description: Invalid media type, size or purpose }
  /technician/work-orders/{workOrder}/signature:
    post:
      summary: Capture a customer signature for an assigned work order
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, signer_name]
              properties:
                file: { type: string, format: binary, description: PNG signature image up to 5 MB }
                signer_name: { type: string, maxLength: 120 }
      responses:
        '201': { description: Signature metadata }
        '403': { description: Missing work-order capability }
        '404': { description: Work order is not assigned to the authenticated technician }
        '409': { description: Work order already has a signature or cannot accept one }
        '422': { description: Invalid signature file or signer name }
  /technician/work-orders/{workOrder}/readings:
    post:
      summary: Record technician readings for an assigned work order
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [readings]
              properties:
                readings: { type: object, maxProperties: 20, additionalProperties: { type: string, maxLength: 120 } }
      responses:
        '200': { description: Updated readings }
        '403': { description: Missing work-order capability }
        '404': { description: Work order is not assigned to the authenticated technician }
        '409': { description: Work order cannot accept new readings }
        '422': { description: Invalid reading payload }
  /technician/work-orders/{workOrder}/materials:
    post:
      summary: Consume bulk material from the technician's van warehouse
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [inventory_item_id, warehouse_id, quantity]
              properties:
                inventory_item_id: { type: integer }
                warehouse_id: { type: integer }
                quantity: { type: string, pattern: '^\\d{1,9}(?:\\.\\d{1,3})?$' }
                note: { type: string, maxLength: 500 }
      responses:
        '201': { description: Consumed material record }
        '403': { description: Missing work-order capability }
        '404': { description: Work order is not assigned to the authenticated technician }
        '409': { description: Insufficient stock or material cannot be consumed }
        '422': { description: Invalid material payload }
  /technician/work-orders/{workOrder}/status:
    post:
      summary: Update the assigned technician's work-order progress
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [en_route, in_progress] }
                at: { type: string, format: date-time }
      responses:
        '200': { description: Updated work-order progress }
        '403': { description: Missing work-order capability }
        '404': { description: Work order is not assigned to the authenticated technician }
        '409': { description: Invalid work-order status transition }
        '422': { description: Invalid status payload }
  /technician/work-orders/{workOrder}/fail:
    post:
      summary: Mark an assigned work order as failed
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, maxLength: 1000 }
                notes: { type: string, maxLength: 5000 }
                reschedule_at: { type: string, format: date-time }
      responses:
        '200': { description: Failed work order with optional reschedule time }
        '403': { description: Missing work-order capability }
        '404': { description: Work order is not assigned to the authenticated technician }
        '409': { description: Work order cannot be failed from its current state }
        '422': { description: Invalid failure payload }
  /technician/work-orders/{workOrder}/complete:
    post:
      summary: Complete an assigned work order idempotently
      parameters:
        - in: path
          name: workOrder
          required: true
          schema: { type: string }
        - in: header
          name: X-Idempotency-Key
          required: true
          schema: { type: string, maxLength: 120 }
      responses:
        '200': { description: Work order completion and resulting service state }
        '403': { description: Missing work-order capability }
        '409': { description: Completion key conflicts with another work order }
  /technician/inventory:
    get:
      summary: List equipment in the authenticated technician's van warehouse
      responses:
        '200': { description: Warehouse, serialized unit, and bulk stock list }
        '403': { description: Missing technician ability }
  /technician/services/{service}/diagnostics:
    get:
      summary: Read tenant-scoped service diagnostics
      parameters:
        - in: path
          name: service
          required: true
          schema: { type: string }
      responses:
        '200': { description: Service state, live session, usage and recent command outcomes }
        '403': { description: Missing technician ability }
        '404': { description: Service not found }
  /webhooks/gateways/{gateway}:
    post:
      security: []
      summary: Receive a signed payment or message gateway webhook
      parameters:
        - in: path
          name: gateway
          required: true
          schema: { type: string }
        - in: header
          name: X-Webhook-Signature
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        '200': { description: Webhook accepted and processed idempotently }
        '401': { description: Invalid webhook signature }
        '422': { description: Invalid webhook body }
  /webhooks/payments/{gateway}:
    post:
      security: []
      summary: Receive a signed online payment gateway webhook
      parameters:
        - in: path
          name: gateway
          required: true
          schema: { type: string, enum: [stripe] }
        - in: header
          name: Stripe-Signature
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        '200': { description: Payment webhook accepted and settled idempotently }
        '401': { description: Invalid Stripe webhook signature }
        '422': { description: Invalid payment webhook body or provider }
  /webhooks/payments/whish/success:
    get:
      security: []
      summary: Verify a Whish success redirect/callback and settle the attempt
      parameters:
        - in: query
          name: externalId
          required: true
          schema: { type: string, pattern: '^[1-9][0-9]*$' }
        - in: query
          name: currency
          required: true
          schema: { type: string, enum: [USD, LBP, AED] }
      responses:
        '200': { description: Provider-confirmed Whish payment settled idempotently }
        '202': { description: Whish payment remains pending }
        '404': { description: Payment attempt not found }
        '422': { description: Invalid or mismatched callback identifiers }
        '502': { description: Whish status could not be verified or posted }
  /webhooks/payments/whish/failure:
    get:
      security: []
      summary: Verify a Whish failure redirect/callback without trusting query status
      parameters:
        - in: query
          name: externalId
          required: true
          schema: { type: string, pattern: '^[1-9][0-9]*$' }
        - in: query
          name: currency
          required: true
          schema: { type: string, enum: [USD, LBP, AED] }
      responses:
        '200': { description: Provider-confirmed Whish payment settled idempotently }
        '202': { description: Whish payment remains pending }
        '404': { description: Payment attempt not found }
        '422': { description: Provider-confirmed failed or mismatched callback identifiers }
        '502': { description: Whish status could not be verified or posted }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Sanctum
  schemas:
    PaymentRequest:
      type: object
      required: [customer_id, amount, currency, method]
      properties:
        customer_id: { type: string, description: Customer public ULID }
        invoice_id: { type: string, nullable: true, description: Invoice public ULID }
        amount: { type: integer, minimum: 1, description: Amount in minor currency units }
        currency: { type: string, minLength: 3, maxLength: 3 }
        method: { type: string, enum: [cash, bank_transfer, card, mobile_wallet] }
        fx_override: { type: boolean, default: false }
        fx_rate_numerator: { type: integer, minimum: 1, nullable: true }
        fx_rate_denominator: { type: integer, minimum: 1, nullable: true }
        fx_override_reason: { type: string, maxLength: 500, nullable: true }
        reference: { type: string, maxLength: 128, nullable: true }
        idempotency_key: { type: string, description: Required for collector batch items }
    PlanChangeRequest:
      type: object
      required: [plan_uuid, effective]
      properties:
        plan_uuid: { type: string, description: Target plan public ULID }
        effective: { type: string, enum: [immediate, next_cycle] }
    RenewalRequest:
      type: object
      required: [preview_id, periods]
      properties:
        preview_id: { type: string, description: Signed short-lived renewal preview token }
        periods: { type: integer, minimum: 1, maximum: 12 }
    CollectorSyncPayment:
      type: object
      required: [customer_uuid, amount, currency, method, idempotency_key]
      properties:
        customer_uuid: { type: string, description: Customer public ULID accepted by offline clients }
        invoice_id: { type: string, nullable: true, description: Invoice public ULID }
        amount: { type: integer, minimum: 1, description: Amount in minor currency units }
        currency: { type: string, minLength: 3, maxLength: 3 }
        method: { type: string, enum: [cash, bank_transfer, card, mobile_wallet] }
        fx_override: { type: boolean, default: false }
        fx_rate_numerator: { type: integer, minimum: 1, nullable: true }
        fx_rate_denominator: { type: integer, minimum: 1, nullable: true }
        fx_override_reason: { type: string, maxLength: 500, nullable: true }
        reference: { type: string, maxLength: 128, nullable: true }
        idempotency_key: { type: string, maxLength: 120 }
    ImportRequest:
      type: object
      properties:
        filename: { type: string, maxLength: 255 }
        csv: { type: string, description: CSV text for JSON clients }
        file: { type: string, format: binary, description: CSV upload for multipart clients }
        dry_run: { type: boolean, default: false }
      description: Provide either csv text or a file upload.
    ImportResponse:
      type: object
      required: [id, type, status, total_rows, successful_rows, failed_rows, report]
      properties:
        id: { type: string }
        type: { type: string }
        filename: { type: string }
        status: { type: string, enum: [preview, completed, rolled_back] }
        total_rows: { type: integer }
        successful_rows: { type: integer }
        failed_rows: { type: integer }
        report: { type: array, items: { type: object } }
